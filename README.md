# Denote

Take notes to compile to a centralized location later.

## Usage

Install:
```bash
deno install --allow-all main.ts
```

New note:
```bash
denote new
```

Compile notes:
```bash
denote compile
```
